name = "ai-news-blog"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Environment variables (set these in Cloudflare dashboard or CLI)
# [vars]
# SITE_URL = "https://your-subdomain.pages.dev"

# Build configuration
[build]
command = "npm run build"
cwd = "."
